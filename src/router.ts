import { Router } from 'express';

const router = Router();

// ─── Rutas de la API ─────────────────────────────────────────────
// Aquí puedes agregar tus rutas, por ejemplo:
// router.use('/users', usersRouter);


export { router };